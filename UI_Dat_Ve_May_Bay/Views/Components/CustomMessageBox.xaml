<Window x:Class="UI_Dat_Ve_May_Bay.Views.Components.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông báo"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Window.Resources>
        <LinearGradientBrush x:Key="VnaPrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0EA5E9" Offset="0"/>
            <GradientStop Color="#0284C7" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270" Color="Black" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="20" Margin="24" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="8" Direction="270" Color="Black" Opacity="0.2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ICON AREA -->
                <Border Grid.Row="0" Margin="0,32,0,16">
                    <Grid>
                        <!-- Background Circle -->
                        <Ellipse x:Name="IconBackground" Width="72" Height="72" Fill="#EEF2FF" HorizontalAlignment="Center"/>
                        <!-- SVG Path for Icon -->
                        <Path x:Name="IconPath" 
                              Data="M12,2A10,10 0 1,0 22,12A10,10 0 0,0 12,2M11,17H13V15H11V17M11,13H13V7H11V13Z"
                              Fill="#6366F1" Stretch="Uniform" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- MESSAGE AREA -->
                <StackPanel Grid.Row="1" Margin="32,12,32,24">
                    <TextBlock x:Name="TxtTitle" Text="Thông báo" FontSize="22" FontWeight="Bold" Foreground="#0F172A" TextAlignment="Center" Margin="0,0,0,14" LineHeight="28"/>
                    <TextBlock x:Name="TxtMessage" Text="Nội dung thông báo ở đây..." FontSize="15" Foreground="#475569" TextAlignment="Center" TextWrapping="Wrap" LineHeight="24"/>
                </StackPanel>

                <!-- BUTTONS AREA -->
                <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,20,20" Padding="24,18">
                    <StackPanel x:Name="PanelButtons" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnYes" Content="Đồng ý" Background="{StaticResource VnaPrimaryGradient}" Style="{StaticResource DialogButton}" Click="BtnYes_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnNo" Content="Hủy" Background="#CBD5E1" Foreground="#1E293B" Style="{StaticResource DialogButton}" Click="BtnNo_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnOk" Content="OK" Background="{StaticResource VnaPrimaryGradient}" Style="{StaticResource DialogButton}" Click="BtnOk_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
