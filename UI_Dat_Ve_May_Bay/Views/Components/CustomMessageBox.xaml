<Window x:Class="UI_Dat_Ve_May_Bay.Views.Components.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông báo"
        Width="450" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <LinearGradientBrush x:Key="VnaPrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00548B" Offset="0"/>
            <GradientStop Color="#008291" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="16" Margin="20" Padding="0,0,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="5" Direction="270" Color="Black" Opacity="0.15"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ICON AREA -->
                <Border Grid.Row="0" Margin="0,30,0,10">
                    <Grid>
                        <!-- Background Circle -->
                        <Ellipse x:Name="IconBackground" Width="64" Height="64" Fill="#EEF2FF" HorizontalAlignment="Center"/>
                        <!-- SVG Path for Icon -->
                        <Path x:Name="IconPath" 
                              Data="M12,2A10,10 0 1,0 22,12A10,10 0 0,0 12,2M11,17H13V15H11V17M11,13H13V7H11V13Z"
                              Fill="#6366F1" Stretch="Uniform" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- MESSAGE AREA -->
                <StackPanel Grid.Row="1" Margin="30,10,30,20">
                    <TextBlock x:Name="TxtTitle" Text="Thông báo" FontSize="20" FontWeight="Bold" Foreground="#1E293B" TextAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock x:Name="TxtMessage" Text="Nội dung thông báo ở đây..." FontSize="15" Foreground="#64748B" TextAlignment="Center" TextWrapping="Wrap" LineHeight="22"/>
                </StackPanel>

                <!-- BUTTONS AREA -->
                <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,16,16" Padding="20,15">
                    <StackPanel x:Name="PanelButtons" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="BtnYes" Content="Đồng ý" Background="{StaticResource VnaPrimaryGradient}" Style="{StaticResource DialogButton}" Click="BtnYes_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnNo" Content="Hủy" Background="#94A3B8" Style="{StaticResource DialogButton}" Click="BtnNo_Click" Visibility="Collapsed"/>
                        <Button x:Name="BtnOk" Content="OK" Background="{StaticResource VnaPrimaryGradient}" Style="{StaticResource DialogButton}" Click="BtnOk_Click"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
